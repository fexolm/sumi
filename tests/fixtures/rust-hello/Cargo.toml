[workspace]

[package]
name = "rust-hello"
version = "0.1.0"
edition = "2024"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

# no_std binary needs -nostartfiles -static -no-pie to produce a clean ET_EXEC
# .cargo/config.toml provides these via rustflags
