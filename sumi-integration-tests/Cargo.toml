[package]
name = "sumi-integration-tests"
version.workspace = true
edition.workspace = true
publish = false
build = "build.rs"

[lib]
path = "src/lib.rs"

[[test]]
name = "test_launcher"
path = "tests/test_launcher.rs"
harness = true

[dependencies]
# For libc::kill(SIGKILL) when a child test hits the wall-clock timeout.
libc = "0.2"

[lints]
workspace = true
